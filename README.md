# Transparent transfer Example

`cargo run -- --rpc <RPC> --source-private-key <SOURCE_PRIVATE_KEY> --target-address <TARGET_ADDRESS> --amount <AMOUNT> --chain-id <CHAIN_ID>`

Optional argument:
- `--memo <MEMO>`
- `--expiration-timestamp-utc <UNIX_TIMESTAMP>`